# EgiyeJai

Vite + React project scaffolded to match the requested teammate structure.

## Run

```bash
npm install
npm run dev
```

## Notes

- Slider placeholder files are included in `src/assets/slider/` and should be replaced with real `.jpg` images.

